[build]
  base = "apps/web"
  command = "npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://dayen-test-deploy.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
